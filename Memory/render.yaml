services:
  - type: web
    name: memory-bot-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python elevenlabs_mcp_quick_setup.py
    envVars:
      - key: PORT
        value: 10000
      - key: ELEVENLABS_API_KEY
        sync: false