# Core dependencies for Memory Bot - Updated for Security

# Web framework (UPDATED for security patches)
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.9.0

# WhatsApp integration
requests==2.32.3

# Azure Speech Services (UPDATED)
azure-cognitiveservices-speech==1.40.0

# Encryption (UPDATED for security)
cryptography==43.0.0

# Environment and configuration
python-dotenv==1.0.1

# JSON and YAML processing (UPDATED)
pyyaml==6.0.2

# Async support
aiofiles==24.1.0

# Logging
colorlog==6.8.2

# System monitoring
psutil==6.0.0
prometheus-client==0.20.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
coverage==7.6.0

# Redis for rate limiting and caching
redis==5.0.7

# Other utilities
ffmpeg-python==0.2.0

# Claude AI Integration (UPDATED for security)
anthropic==0.39.0

# Additional security dependencies
python-jose[cryptography]==3.3.0  # For JWT
passlib[bcrypt]==1.7.4  # For password hashing
email-validator==2.2.0  # For email validation

# Rate limiting
slowapi==0.1.9  # FastAPI rate limiting

# Monitoring and observability
sentry-sdk[fastapi]==2.10.0
opentelemetry-api==1.26.0
opentelemetry-sdk==1.26.0

# Input validation
python-multipart==0.0.9
bleach==6.1.0  # For HTML sanitization